from aiogram import Router, F
from aiogram.types import Message

from handlers.command_admin import is_admin
from handlers.command_mod import is_mod

help_router = Router()

HELP_TEXT = """Привет! Я <b>БотИК</b>. Вот что я умею:

🎲 <b>Случайный активист:</b>
<code>!кто</code> [текст] — выясняет, кто в чате [текст]
<code>!сантехник дня</code> — выбирает лучшего сантехника (работает один раз в сутки)

👤 <b>Информация об активисте:</b>
<code>!инфо</code> [@тег | фамилия] — показывает анкету активиста по тегу или фамилии (либо ответь <code>!инфо</code> на его сообщение)

💬 <b>Цитаты:</b>
<code>!цитата</code> — ответь на сообщение или перешли сообщение (можно несколько), чтобы сохранить цитату
<code>!мудрость</code> — выдаёт случайную цитату из архива
<code>!топ цитат</code> [неделя | месяц] — три лучшие по сердечкам, голосуй кнопкой под цитатой
<code>!батл</code> — 10 пар цитат, выбираешь лучшую; итог — победитель
<code>!топ батл</code> — топ цитат по баллам за выигранные пары

📊 <b>Статистика чата:</b>
<code>!топ</code> [сегодня | неделя | месяц] — топ-10 болтунов чата
<code>!стата</code> [@тег | фамилия] — сколько человек написал сообщений (либо ответь <code>!стата</code> на его сообщение)
<code>!топ кармы</code> — топ-10 по спасибам (ответь <code>спасибо</code> / пасиб / спс на сообщение — автору капнет +1)

🎴 <b>Таро:</b>
<code>!расклад</code> — карта дня и прогноз (одна карта на весь день, назавтра новая)

⚔️ <b>Дуэли:</b>
<code>!дуэль</code> [@тег] — вызвать на дуэль (или ответь на сообщение); проигравший молчит час
<code>!матдуэль</code> — математическая дуэль ответом: сумма трёх трёхзначных, кто первый; молчание 10 минут
<code>!рулетка</code> — испытать удачу: шанс 1 к 6 умереть на час
<code>!поднять флаг</code> / <code>!опустить флаг</code> — белый флаг: защита от дуэлей на 3 дня
<code>!мирные</code> — кто под белым флагом
<code>!кладбище</code> — кто мёртв и когда воскреснет
<code>!топ дуэлей</code> — топ-3 победителей и топ-3 проигравших
<code>!маттоп</code> — топ матдуэлей

📣 <b>Общий сбор:</b>
<code>!все</code> [текст] — тегает всех, кого я видел в этом чате

📝 <b>Анкета:</b>
<code>!обо мне</code> — анкета: посмотреть, изменить, удалить (в личке с ботом)

✨ <b>Разное:</b>
<code>!вероятность</code> [событие] — высчитывает вероятность предложенного события
<code>!подскажи</code> — помогает определиться в важных решениях
<code>!ботбрат</code> — брат бота или бот брата, брат
<code>!помощь</code> — вызывает это сообщение"""


ADMIN_HELP = """🛠 <b>Только для админов:</b>
<code>!админка</code> — состав в Excel: выгрузить, поправить, залить обратно; кто привязал телеграм
<code>!id</code> — узнать свой telegram id"""


MOD_HELP = """🔇 <b>Модерация:</b>
<code>!модерация</code> — замутить и размутить четверых, заместитель"""


async def with_admin_block(text: str, message: Message) -> str:
    """Дописать админский блок, если спрашивает админ и спрашивает в личке.

    Именно в личке: !помощь в группе читает весь чат, и упоминание панели
    там выдало бы её существование всем — а она задумана незаметной.
    """
    if message.chat.type != "private" or message.from_user is None:
        return text
    if not await is_admin(message.from_user.id):
        return text
    return f"{text}\n\n{ADMIN_HELP}"


async def with_mod_block(text: str, message: Message) -> str:
    """Строка про модерацию — только своим и только в личке.

    Как админский блок выше: в группе упоминание панели выдало бы
    её существование всему чату.
    """
    if message.chat.type != "private" or message.from_user is None:
        return text
    if not await is_mod(message.from_user.id):
        return text
    return f"{text}\n\n{MOD_HELP}"


@help_router.message(F.text == "!помощь")
async def help_cmd(message: Message):
    text = await with_admin_block(HELP_TEXT, message)
    await message.answer(await with_mod_block(text, message), parse_mode="HTML")
